---
title: Command
description: A flexible command menu component for keyboard-centric navigation, enabling quick access to actions and options through a streamlined, customizable interface.
---

<Preview
  component={<CommandExample />}
  settingsEngine={<CommandSettingsEngine />}
  componentName={'CommandExample'}
/>

### Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash npx shadcn add https://extend-ui.com/registry/command.json ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>
        Install the following dependencies: ```bash npx install
        @radix-ui/react-dialog ``` ```bash npx shadcn@latest add dialog ```
      </Step>
      <Step>
        Copy and paste the following code into your project.
        <ComponentSourceLive componentName={'Command'} />
      </Step>
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

### Command component props

{' '}

<div className="my-6 w-full overflow-x-auto">
  <table className="w-full border-collapse border border-gray-300 bg-white break-words">
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>children</td>
        <td>React.ReactNode</td>
        <td>
          Defines the nested elements or components, typically a combination of{' '}
          <code>CommandItem</code>, <code>CommandGroup</code>, and others.
        </td>
      </tr>
      <tr>
        <td>className</td>
        <td>string</td>
        <td>Additional CSS classes to style the root Command component.</td>
      </tr>
      <tr>
        <td>ref</td>
        <td>React.Ref&lt;HTMLDivElement&gt;</td>
        <td>
          A ref to the root Command container for DOM manipulation or reference.
        </td>
      </tr>
      <tr>
        <td>...props</td>
        <td className="break-all">
          React.ComponentPropsWithoutRef&lt;CommandPrimitive&gt;
        </td>
        <td>
          Additional props that can be passed to the root Command component.
        </td>
      </tr>
    </tbody>
  </table>
</div>
